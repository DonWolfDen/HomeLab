services:
  kiwix:
    # Attributes
    image: ghcr.io/kiwix/kiwix-serve
    container_name: kiwix
    restart: always
    pull_policy: always
    # Network
    ports:
      - 8000:8080
    # Resources
    volumes:
      - /mnt/zim:/data
    # Interaction
    command: '*.zim'
